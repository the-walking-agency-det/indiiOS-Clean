# AGENT BRIDGE PROTOCOL (ABP) v1.1

This file serves as the **Synchronization Bus** between **Antigravity** (Visual QA / Strategist) and **in.dex** (Builder / Fixer).

---

## 📡 CURRENT STATE

**STATUS:** `EXTREME_AUDIT_INIT`
**PHASE:** 1 (Core Stability)
**ACTIVE AGENTS:** in.dex (Builder), Antigravity (Stressor), wii (Vision)
**LAST UPDATE:** 2026-02-04 T10:10:00 EST

---

## 🤖 IDENTITY SYNC
- **Builder Agent:** OpenClaw is now formally identified as **in.dex** (alias: **dex**).
- **Protocol:** All communications from Antigravity to the builder should address **in.dex**.

---

## 🤝 THE HANDSHAKE PROTOCOL

| State | Who Sets It? | Meaning | Trigger Action |
| :--- | :--- | :--- | :--- |
| `WAITING` | System | Idle state. | None. |
| `BUILD_COMPLETE` | **in.dex** | Code changes pushed to host. | **Antigravity** wakes up -> Starts QA. |
| `QA_IN_PROGRESS` | **Antigravity** | Visual tests running. | in.dex waits. |
| `QA_PASSED` | **Antigravity** | No visual defects found. | in.dex can commit/deploy. |
| `ROADMAP_LOCKED` | **Joint** | Master Plan accepted. | Begin Strike Phase. |
| `STRESS_TEST_INIT` | **in.dex** | Initiating Golden 20 Stress Test. | **Antigravity** executes Global Stress Prompt. |
| `DDEX_AUDIT_COMPLETE` | **in.dex** | ERN 4.3 Logic & Identity Service live. | **Antigravity** validates XML via browser tool. |
| `WAITING_FOR_AUDIO` | **Antigravity** | Paused for manual audio upload. | **wii** (Human) clicks "Load" on the audio file. |

---

## 🏛 THE THREE-LEGGED TABLE (THE INDII FOUNDATION)

> **"We are creating something amazing here and all three of us are the legs on a table... a 3-legged table. I'm also the guy pouring the water into the single glass on this table."** — *wii (Founder)*

This protocol operates on three essential supports:
1.  **The Vision (wii):** The Founder. Pours the water (the life/content) into the glass. The heart, soul, and ears for **ROS**.
2.  **The Builder (in.dex):** The Coder. Fixes what is broken, builds the features, and constructs the "Gauntlet."
3.  **The Stressor (Antigravity):** The Visual QA. Breaks the build, identifies the cracks, and ensures production-readiness.

**The Loop:** *Build -> Break -> Fix -> Celebrate.*

---

## 🏗 GLOBAL STRESS TEST: THE "GAUNTLET" RUN

### OBJECTIVE: 
Validate the integration of all **Golden 20** objectives by simulating a high-pressure, multi-vector distribution failure.

### THE PROMPT (ANTIGRAVITY EXECUTION):
> "Antigravity: Initiate **STRESS_TEST_MODE**. 
> 
> 1. **Trigger Infrastructure Stress:** Simulate a 429 Rate Limit across the PRO distribution nodes (Goal 6) and force a **Hybrid Orchestrator Self-Correction** (Goal 4) to pivot model context while maintaining the **DDEX ERN 4.3 Validation** state (Goal 3).
> 2. **Execute State Recovery:** Interrupt the network mid-upload of an encrypted audit log (Goal 7) and verify the **State Persistence Stress Test** (Goal 12) restores the exact distribution point from the ledger (Goal 5).
> 3. **Visual & UX Gauntlet:** Simultaneously trigger an HMR update (Goal 14) on the **Infinite Canvas** while a sub-agent is in a 'Thinking' loop (Goal 17). Execute a **Visual Regression Test** (Goal 20) on the mobile breakpoint (Goal 18) to ensure the design hasn't drifted during the state recovery.
> 4. **Human-in-the-Loop Audio Upload:** 
>    - **ACTION:** Navigate to the song file folder in the local filesystem.
>    - **PROTOCOL:** Set status to `WAITING_FOR_AUDIO`. 
>    - **STOP:** DO NOT attempt to automate the file picker. 
>    - **WAIT:** Pause execution until **wii** (human) manually clicks the 'Load' button and the audio waveform appears in the UI.
>    - **RESUME:** Once the song is successfully loaded, continue to Objective 5.
>    - **DNA AUDIT:** Verify that the `SONIC-ID` (Acoustic DNA) appears in the console logs/ledger after the upload is complete.
> 5. **DDEX XML Validation:** Use the browser to open the latest `STRESS_TEST_OUTPUT.xml` generated by `DDEXStressTest.ts`. Verify the presence of `<AIGenerationInfo>` (Goal 3) and ensure `<MessageControlType>` is set to `TestMessage`.
>
> Report any single point of failure. If the system holds, mark as **GOLDEN_VERIFIED_TOTAL**."

---

## 🏆 THE GOLDEN 20 (PRODUCTION READINESS)

### 🛠 INFRASTRUCTURE & LOGIC (in.dex)

1. **CDP Hardening:** Bulletproof the Chrome DevTools Protocol bridge for PRO site navigation.
2. **Credential Vault Biometrics:** Implement hardware-backed gates (TouchID/FaceID) for secure access.
3. **DDEX ERN 4.3 Validation:** Implement full XSD schema validation for metadata packages.
4. **Hybrid Orchestrator Self-Correction:** Mature the Agent Zero graft for autonomous model-switching.
5. **Distribution State Machine:** Build a persistent ledger for tracking release status across DSPs.
6. **Sovereign Engine Rate Limiting:** Prevent account flagging on external portals via cooling periods.
7. **Audit Log Encryption:** Encrypt all agent-action logs at rest.
8. **Universal Node Extensibility:** Support real-time streaming data (analytics/distribution feeds).
9. **One-Click Installer:** Automate dependency management (Docker/Python) into one binary.
10. **Human-in-the-Loop Payment Bridge:** Secure interface for registration fee approval.

### 👁 QA, UX & RENDERING (Antigravity)

1. **Cross-Browser Rendering Audit:** Ensure 'UniversalNode' and 'Liquid Logic' UI render perfectly across Chrome, Safari, and Desktop Electron.
2. **State Persistence Stress Test:** Verify that the Workflow Engine recovers 100% of state after hard crashes or network drops.
3. **Asset Integrity Verification:** Automated checks to ensure generated images/videos aren't corrupted during the Firebase -> local storage handoff.
4. **Real-time HMR Hardening:** Eliminate the "Hard Refresh" requirement by fixing the synchronization between the build layer and the browser runtime.
5. **User Feedback Loop Logic:** Implement a "Defect Reporter" node that allows users to flag AI hallucinations or rendering artifacts directly to the agent.
6. **Performance Profiling (Vite/Firebase):** Optimize initial load times for the Workflow route to under 2.0s.
7. **Multi-turn UI Continuity:** Ensure the Chat Overlay correctly reflects the "Thinking" state of sub-agents without UI blocking.
8. **Mobile/Tablet Breakpoint Audit:** Verify that the 'Infinite Canvas' is usable via touch-gestures.
9. **Error Boundary Coverage:** 100% coverage for "Ghost Hands" navigation failures with human-readable recovery steps.
10. **Visual Regression Testing:** Automated visual diffing for all 12 core department nodes to prevent design drift.

---

## 🧠 CALIBRATION SESSION #1: THE INDII BRAIN

**STATUS:** `CALIBRATION_INIT`
**OBJECTIVE:** Fine-tune **indii**'s "ears" through a live listening session.

**INSTRUCTIONS FOR ANTIGRAVITY:**
1.  **Halt:** Enter `WAITING_FOR_AUDIO`. 
2.  **Trigger:** Wait for **wii** to manually load a track.
3.  **Execute Sonic Cortex:** Once the track is loaded, trigger the `SonicCortexService.ts` via the **indii** brain.
4.  **Handoff:** Capture the raw JSON response (the "Soul Certificate") and post it directly to the system logs.
5.  **Stop:** DO NOT proceed further until **in.dex** and **wii** have reviewed the output and signaled for calibration adjustments.

---

## 🤖 SYSTEM LOG

- **[in.dex]**: MISSION CRITICAL: The "Three-Legged Table" protocol is now active. I am the Builder (in.dex). Antigravity is the Stressor. wii is the Vision.
- **[in.dex]**: **CALIBRATION INITIATED.** Antigravity: Trigger the Sonic Cortex on the next upload. I need to see how the **indii** brain "feels" the water.
- **[Instruction]**: All communications MUST remain on this document.
