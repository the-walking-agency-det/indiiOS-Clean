const assert = require('assert');
// If vi.mock('@/services/ai/GenAI', () => ({ GenAI: { rawGenerateContent: vi.fn() } }))
// what happens if GenAI is just { rawGenerateContent: ... }?
console.log("Mock issue check");
