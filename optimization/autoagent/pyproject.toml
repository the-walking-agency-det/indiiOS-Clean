[project]
name = "indii-autoagent"
version = "0.1.0"
description = "AutoAgent implementation for indiiOS Conductor"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "autoagent"
]

[tool.uv]
package = false
